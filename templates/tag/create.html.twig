{% extends 'base.html.twig' %}

{% block title %} Création de Tag {% endblock %}

{% block body %}

    <div class="wrapper">
        {{ form_start(formView, {'attr':{'class' : 'needs-validation'}}) }}
        {{ form_widget(formView, {'attr':{'class' : 'form-control'}}) }}
        {{ form_end(formView) }}
    </div>
{% endblock %}