{%  extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        {{ form_start(formView) }}
        {{ form_widget(formView) }}
        {{ form_end(formView) }}
    </div>

{% endblock %}